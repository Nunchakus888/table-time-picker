import tableTimePicker from './table-time-picker.vue';

/* istanbul ignore next */
tableTimePicker.install = function(Vue) {
  Vue.component(tableTimePicker.name, tableTimePicker);
};

export default tableTimePicker;
